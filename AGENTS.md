# 项目协作准则

## 错误必须帮助定位问题

用户明确要求：“报错最好有直接原因，而不是笼统的说啥，这样以后调试就会方便，变成准则”。后续功能和修复均遵守：

- 已知失败必须显示直接原因、失败阶段、稳定错误代码及可操作的下一步；不得只显示“失败，请稍后重试”。
- 已知页码、已读条数、预期/实际总数、等待时长等现场信息应随错误保留，不能在外层 catch 中丢失。等待重试期间的权限错误、页面关闭等异常不能被统一改写成超时。
- 只陈述已观测事实。超时不能凭空断言是网络、登录或学校风控；不能识别的异常须明确说明原因未知，并保留安全的阶段、操作及异常类型。
- 界面应能查看和复制排错信息。复制成功后再提示成功；复制失败时保留可手动选择的文本。恢复成功后清除旧错误。
- 诊断只保留允许的结构字段和固定文本。账号、Cookie、会话 URL、姓名、原始页面内容、原始异常消息及堆栈不得进入本机持久诊断、剪贴板报告、发布包或日志。
- 分页明细失败仍可显示已读取的学校汇总，但必须标注来源并暂停实时估算；不得将部分记录标为完整成功。登录失效暂停自动采集。
- 新增错误分支时补充有意义的回归测试，检查原因和现场信息能经过“抛出 → 捕获 → 缓存清洗 → 界面/复制”保留，同时验证敏感内容被排除。

## 验证与交付

- 保持串行翻页、1.5 秒间隔、50 页上限和 30 分钟同步。
- 使用虚构记录测试，不把真实学校记录写入仓库。
- 按任务运行 `npm test`、`npm run audit:release`、`npm run build` 和 `npm run test:install`；最后安装测试必须使用最终 ZIP。
- 新增源码须列入 `release-files.json`；发布 ZIP 只包含明确允许的运行文件。
