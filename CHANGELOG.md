# 更新记录

## 1.5.1 · 2026-09-20

- 首次正式发布 iPhone / iPad Safari Userscripts 手动版，包含下方 1.5.0 的功能；同一 Release 统一提供 Chrome 扩展 ZIP、Android APK、Safari 脚本与安装 ZIP，版本均为 v1.5.1，并提供统一的 SHA-256 校验文件。
- Chrome / Android 沿用 v1.4.2 的功能与修复；Android 保持原发布签名，支持覆盖升级。
- 根据 iPhone 试用反馈，安装说明明确要求登录前为学生系统与登录站点分别请求桌面网站，并补充手机模式登录出错后的重新进入步骤。
- Safari 面板首次使用与登录提示同步说明桌面网站要求。
- 补充停用及恢复方法：关闭脚本后刷新学校网页或关闭旧标签页；停用不清除学校登录、桌面网站设置或本机考勤缓存。

## 1.5.0 · 并入 1.5.1 发布

- 新增 iPhone / iPad Safari Userscripts 手动版，使用 Safari 学校登录与本机考勤缓存，复用共享采集、计时和界面。
- 导出单个自包含 `.user.js` 及安装 ZIP，不需要 Apple 开发者会员或每周签名。
- 记录可观测的内嵌访问限制、页面安全策略、加载错误、前台中断及存储错误；保留分页现场和上次完整快照，提供诊断复制与手动选择。
- 新增 Chromium / WebKit 虚构学校流程和最终 ZIP 安装验证；发布前完成 iPhone 试用。

## 1.4.2 · 2026-09-20

- 首次正式发布独立安卓应用，包含下方 1.4.0、1.4.1 的全部改动；同时提供同版本 Chrome 扩展。

- 根据实际学校页面修复无文字 `.layui-none` 且无分页栏的正常空表识别，避免凌晨第二个自然日零记录导致前一天完整明细无法保存；仅接受渲染后的主表空标记，加载中、错误提示和不明表格继续保留失败状态。
- 排错信息增加正在查询的日期、页面筛选日期和固定表格状态，经过缓存清洗与复制仍保留现场，不收集原始页面、账号或会话 URL。
- 修复安卓学校页面禁止缩放、只能看到左上角的问题：使用桌面宽度并默认适配屏幕，允许双指缩放，提供原生“缩小／放大”按钮；返回考勤继续刷新一次。
- 增加无文字空表跨午夜合并、诊断传递与隐私过滤、原生 WebView 缩放及远端控件触摸回归测试；使用虚构记录。

## 1.4.1 · 并入 1.4.2 发布

- 安卓点击“返回考勤”时附带刷新一次，复用刷新按钮的进度和错误提示；重复点击不重复采集，普通前台恢复仍只展示缓存。
- 将已加载的“暂无数据”及零条明细识别为正常空结果，忽略空列表遗留的下一页控件；加载中、未知表格和条数不齐仍不能标为成功。
- 明细请求优先使用 90 条／页，并通过学校分页选项同步 Ajax 页大小；超过 90 条继续翻页，保持 1.5 秒间隔和跨日共享请求上限。
- 修复第二个自然日的第一页被排错信息标成第二页；补充空结果恢复、午夜合并、90 条跨页、最终 ZIP 与 Android WebView 回归验证。

## 1.4.0 · 并入 1.4.2 发布

- 修复 issue #4 的空 JavaScript 分页链接 CSP 报错，新增最终 ZIP console/CDP 安全日志回归测试；真正依赖脚本 URL 的分页给出具体诊断。
- 新增独立 Android APK，使用本机 WebView 学校会话及桌面浏览器标识，复用桌面界面和共享采集／统计代码。安卓仅手动刷新，登录过期后重新登录。
- 移除 companion、局域网权限、配对入口及旧服务构建；保留旧 README、隐私说明、使用文档和既有 Release。

## 1.3.0 · 2026-09-19

- 桌面小窗改为居中圆环、单层滚动和系统明暗主题；圆环、时长与底部实际在校分段统一使用校徽粉红色。
- 使用学校官网校徽，多尺寸扩展／工具栏／窗口图标兼顾 Windows 和 Retina；macOS Dock 仍可能归属 Chrome，无原生包装。
- 月记录支持日历／列表并记住选择，达标绿点包含节假日，未达标黄点仅用于工作日；详情与月度统计按需展开，缺失及未来数据不误标为未达标。
- 手机连接默认折叠，标题显示状态，保留后台连接与配对；异常可展开复制。
- 考勤日统一为北京时间 05:00 至次日 05:00，连续进取最晚、连续出取最早。跨 05:00 的整段作废，包括恰好 05:00 出校；未闭合估算不跨日延续，历史仍采用学校汇总。
- 凌晨串行读取两个自然日，全部完成后更新快照；跨切日采集、分页失败均不能误报新日成功。保持 30 分钟同步、1.5 秒间隔及两天合计 50 页上限。
- 状态升级为 schemaVersion 5，保留历史、配对与视图偏好，旧今日快照等待重新同步；扩展与伴随服务须一起更新。
- 手机端界面未作新版视觉适配，待更新；共享统计和缓存模块已同步升级，接口、端口和权限不变。
- 增加午夜、05:00、跨月年、重复刷卡、失败冻结、迁移、月历标点、键盘及双主题验证；启动器测试隔离端口，不干扰正在运行的手机服务。

## 1.2.0 · 2026-09-12

- 增加局域网手机网页查看与“刷新学校数据”，提供内置 Node 的 Windows x64／macOS arm64 伴随服务包，无需租服务器或另装 Node。
- 修复连续同向刷卡的最小闭包计算：连续进门取最晚一次，连续出门取最早一次，计时与状态使用相同有效事件。今天按完整明细估算，历史使用学校汇总。
- 保存今日完整快照；明细失败、登录失效及数据过期时暂停估算。保留直接原因和可复制的脱敏诊断，成功恢复后清除旧错误。
- 修复切换 Wi-Fi／网卡后服务仍监听旧地址、却生成新地址链接的问题；start 核对实例并重新绑定，验证地址后生成链接，保留原配对凭据。
- 修复月初旧月份混入今日日期的问题；补齐整月日期，区分待同步与未来占位，缺失日期不当作零时长或计入历史达标统计。
- 手机支持完整链接恢复、可选记住查看权限及退出；连接设置与排错工具默认折叠，自动复制受限时可长按手动复制。
- 增加 Mac／Windows 网络检测启动器和手机检测入口，报告固定错误代码与现场信息，不包含账号、令牌、IP 或原始考勤。
- 简化 README 安装流程，新增手机查看与排错文档，更新虚构数据演示图；修正 macOS WebKit 安装命令。
- 源码启动器缺少内置运行时时显示 `RUNTIME_MISSING`、下载与完整解压步骤，以及开发启动命令，避免误把源码当安装包使用。
- 保持学校每 30 分钟同步、串行翻页、1.5 秒间隔与 50 页上限；增加网络切换、日历边界和最终 ZIP 的双平台回归验证。

## 1.1.2

- 修复 1.1.1 未识别学校 Layui 箭头分页而误报明细不完整的问题；支持当前页、末页禁用按钮及异步加载状态。
- 只读取 Layui 主表，避免固定列副本重复计数；保留多页合并、宿舍过滤、1.5 秒间隔和 50 页上限。
- 明细失败时显示已读取的学校每日汇总并暂停实时估算，保留上次完整成功时间，单独标注汇总更新时间。
- 错误提示显示直接原因和建议操作；新增可查看、复制的排错信息，保留失败阶段、错误代码、已知页码、条数、等待时长和发生时间。
- 补齐入口缺失、采集页关闭、脚本权限、缓存读写和小窗后台断连等错误；读取重试不会把权限错误或脚本失效统一吞成超时，登录跳转会直接提示登录。
- 无法分类的错误明确标为原因未知；仅保留允许的结构字段，不保存原始异常、账号或会话地址。成功后清除旧诊断。
- 将错误必须便于定位的要求写入 `AGENTS.md`，补充诊断传播、复制失败回退和敏感信息过滤测试。
- 新增 23 条、3 页 Layui 测试和 ZIP 安装后的实际扩展测试，覆盖整页宿舍记录、异步翻页、超时、总条数变化及重试恢复。

## 1.1.1

- 当天刷卡记录逐页读取后排序、去重，修复较早进出记录落在后续页时漏算的问题。
- 宿舍记录不计时，但不会因为整页都是宿舍记录而停止翻页。
- 保持每 30 分钟同步；同次同步内串行翻页，每次间隔 1.5 秒，最多 50 页。
- 翻页失败、总数变化或读取不完整时保留上次数据并提示失败，不保存部分结果。

## 1.1.0

- 首个公开开源版本：MIT 许可、中文安装说明及隐私说明。
- 发布文件采用明确清单，演示与测试全部使用虚构数据。
- 本地缓存按允许字段保存并迁移，报错不暴露会话 URL。
- 保留学校道闸计时、每秒本地续算和 30 分钟同步。
- 登录过期时暂停自动采集，重新登录后恢复。
