# 隐私说明

此扩展在使用者自己的 Chrome 中运行。默认只在浏览器运行，没有 AI 调用、遥测、广告或第三方数据上传。启用可选伴随服务后，脱敏状态会保存到自己的电脑，并可供持查看令牌的局域网设备读取。

## 登录与数据

- 每位使用者在学校系统登录自己的账号。扩展没有密码输入框，也不读取密码管理器。
- 学校 Cookie 由浏览器管理。扩展不申请 cookies 权限，不导出或保存 Cookie。
- 查询时从当前学校页面临时识别学号与会话路径，只用于该次学校请求。它们不会写入扩展缓存、日志、发布包或 GitHub。
- 本机扩展存储仅保存本月每日日期、类型、时长与达标状态、当天学校道闸进出时间和方向，以及刷新状态、更新时间、窗口编号和经过允许字段筛选的最近一次诊断。诊断包括固定错误类型、阶段、操作、异常类别、已知页码/条数/等待时长及发生时间。不会保存姓名、学号、门禁通道名称、原始异常消息/堆栈或完整页面内容。
- “复制排错信息”仅在用户点击时把上述诊断和扩展版本复制到本机剪贴板，不会自动发送给维护者或任何第三方。诊断不包含原始进出记录、账号、Cookie 或会话 URL；成功刷新后会清除缓存中的旧诊断。
- 考勤缓存使用 chrome.storage.local，不使用 chrome.storage.sync，不随源码打包。该缓存不是加密保险箱，有本机浏览器访问权限的人可能查看它。
- 升级会按允许字段重新保存旧缓存并移除扩展存储中不再使用的字段。卸载扩展可清除其本地缓存；这不会注销学校账号。学校 Cookie 可在 Chrome 的网站数据设置中自行删除。

## 网站权限

stu.slai.edu.cn：打开学校系统并读取本人考勤页面。

sts.slai.edu.cn：识别学校单点登录跳转。登录由用户在学校页面完成。

扩展向上述学校域名发起学校查询；可选权限 `http://127.0.0.1/*` 仅在用户启用手机查看时申请，推送固定发往 `http://127.0.0.1:32101/api/state`，手动刷新通路固定连接 `ws://127.0.0.1:32101/api/control`；该连接每 20 秒交换一次本机保活消息。学校网页自己的资源和单点登录行为由学校控制。学校系统可能把会话标识放在 URL 中，浏览器正常导航可能留下历史记录，扩展不会额外申请历史记录权限或自动清除浏览器历史。

## 可选的局域网服务

- 保存的完整快照只含日期、脱敏进出方向与时间、完整更新时间。今日失败时仅展示该快照，不把不完整记录拼入其中。
- 扩展单独保存本机写入令牌、开关和安全的桥接诊断；它们不进入导出的考勤状态。服务独立生成写入令牌与查看令牌，保存在上述用户数据目录的配置文件中。
- 写入口只监听本机回环地址。查看口监听回环与选定的私有局域网 IPv4，所有状态读取和手动刷新请求需要查看令牌；手机不会得到学校账号、Cookie、会话链接或写入令牌。
- 当前版本为 HTTP，只在可信 Wi-Fi 使用。令牌不是传输加密，能访问该网络流量的其他人可能读取传输；不要开放公网端口或分享查看链接。
- 手机默认只在当前标签页会话中保存查看令牌。用户勾选“在此浏览器记住查看权限”且服务验证成功后，该地址的浏览器本地存储会保存查看令牌，供此浏览器的新标签页读取。取消勾选可清除记住的令牌；“退出并清除保存的权限”同时清除本页内存、会话及该地址的已记住权限，其他已经打开的标签页需一并关闭。清除站点数据、换浏览器或服务地址改变后需重新连接。
- 手机考勤状态只保留在当前页面内存；服务重启保留本机最后状态与原始更新时间。不会用接收时间伪装学校新同步。粘贴的其他地址链接不会被本页用于请求。
- 手机顶部“刷新学校数据”向当前服务的 `POST /api/refresh` 发送随机请求编号；请求不能携带考勤、学校地址或任意命令。服务验证查看令牌与同源请求后，经仅在本机的独立写入令牌通路通知扩展采集。手机不能写入 `POST /api/state`，也不能替电脑登录学校。
- 手动刷新编号、执行状态和脱敏结果诊断仅在本次服务进程内存中保留；合并正在执行的请求并记住最近 32 个编号以防重试重复采集，重启后不重放。平时手机每 15 秒读取状态，手动刷新期间每秒读取进度；这些状态读取本身不触发采集。
- 自动复制受限时，手动复制文本框在页面内存中保留点击时的脱敏报告快照；浏览器每秒计时与定期读取不会改写该文本框。它不含查看令牌或粘贴的链接，关闭文本框后移除。
- 本机连接信息页含配对码及查看链接，只用于本人配置，不属于诊断报告，不应上传或打包。服务器不记录请求体、令牌、原始异常或访问日志。诊断仅使用固定错误代码、阶段及允许的数字字段。
- 可手动运行 Mac／Windows 网络检测。它只向本机回环地址和已选且仍属于本机的局域网地址发送只读探测，并查询系统网络类型及此程序的防火墙规则，不扫描其他设备、不访问学校、不自动修改配置。系统返回的地址、网络名称、程序路径及异常文本只在内存中判断；报告只保留固定代码与允许的数字字段，不包含密钥、IP、网络名称、账号、用户名、路径或考勤明细。脱敏报告保存在本机用户数据目录，只有使用者手动复制后才会离开本机。
- 停止、取消自动启动及删除本机数据目录的步骤见 README。扩展中关闭手机查看会停止后续推送和接收新的手动刷新请求；服务已保存的缓存需在本机删除。

## 开源与反馈

仓库中的账号、记录和演示图均为虚构测试数据。GitHub 用于代码托管和手动下载，扩展运行时不会向 GitHub 上传数据。

提交 Issue 时请删除姓名、学号、会话 URL、Cookie、原始考勤截图和网络日志。说明现象、扩展版本与浏览器版本即可。
