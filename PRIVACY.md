# 隐私说明

Chrome 扩展和独立 Android 应用直接在使用者自己的设备上运行，没有 AI 调用、遥测、广告或第三方考勤上传。当前版本不再提供局域网 companion；旧版行为见[历史隐私说明](docs/legacy-v1.3.0-PRIVACY.md)。

## 学校登录

- 每位使用者在学校提供的页面登录自己的账号。应用没有自制的密码表单，不读取密码或密码管理器。
- Chrome Cookie 由浏览器管理；扩展没有 `cookies` 权限。Android Cookie 由本机 WebView 管理，登录页与采集页共享同一会话，不把 Cookie 导出给 JavaScript 界面、文件报告或其他设备。
- Android 登录和采集使用桌面浏览器标识，以请求学校桌面页面；支持时同时设置桌面 Client Hints。不会绕过登录、证书验证或学校访问权限。
- 学号与会话路径只在该次查询中临时使用，不写入应用考勤缓存、诊断、日志或 GitHub。学校页面自己的存储、资源及登录行为由学校控制。

## 本机数据

- 考勤缓存只保留允许字段：月度日期、类型、时长、达标状态，当前考勤日的道闸进出时间和方向，最后完整快照、更新时间及脱敏诊断。
- 不保留姓名、学号、道闸原始名称、学校页面内容、原始异常消息／堆栈或会话 URL。
- Chrome 使用 `chrome.storage.local`，不启用同步存储。Android 使用应用私有文件 `attendance.json` 和本机视图偏好，不申请外部文件访问权限；应用关闭系统备份。
- 更新保留可靠缓存，升级时按允许字段清洗旧格式。超过 35 分钟或采集失败时暂停估算；不使用不完整明细冒充最新完整数据。
- Android 不安排后台采集。打开应用只读取本机缓存，点击刷新或学校页的“返回考勤”才开始一次学校查询；离开前台中断采集。退出学校账号会清除应用内学校会话和考勤缓存。卸载或清除应用数据也会删除本机数据。
- 卸载 Chrome 扩展会清除扩展存储，学校网站的登录 Cookie 仍可在 Chrome 网站数据设置中管理。

## 权限与隔离

扩展只访问 `https://stu.slai.edu.cn/*` 和 `https://sts.slai.edu.cn/*`；当前版本没有 localhost 权限、局域网端口或配对令牌。

Android 只申请网络权限。考勤 UI 来自随 APK 打包的本机资源，学校页面放在独立 WebView 中。原生通信仅绑定本机 UI 的固定来源和主框架，学校页没有原生存储或文件接口。只向固定学校 HTTPS 域名导航；不会放宽 CSP、允许明文 HTTP 或忽略 SSL 错误。

## 诊断与反馈

已知错误提供直接原因、失败阶段、稳定代码、可操作建议及允许的数字现场。未知错误明确标为未知，不凭超时推断学校风控或网络故障。原始学校内容、Cookie、URL、账号和异常堆栈不进入诊断。

“复制排错信息”只在使用者点击时复制脱敏诊断；成功后才显示已复制，失败则保留可手动选择的文本。不会自动发送给维护者。成功恢复后清除旧错误。

GitHub 用于代码托管和手动下载安装包，运行时不向 GitHub 上传考勤。仓库测试、演示截图和仪器测试全部使用虚构记录，测试资产不进入运行 APK。反馈时只需现象、版本和脱敏排错信息。
