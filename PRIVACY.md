# 隐私说明

此扩展在使用者自己的 Chrome 中运行。没有服务器、AI 调用、遥测、广告或第三方数据上传。

## 登录与数据

- 每位使用者在学校系统登录自己的账号。扩展没有密码输入框，也不读取密码管理器。
- 学校 Cookie 由浏览器管理。扩展不申请 cookies 权限，不导出或保存 Cookie。
- 查询时从当前学校页面临时识别学号与会话路径，只用于该次学校请求。它们不会写入扩展缓存、日志、发布包或 GitHub。
- 本机扩展存储仅保存本月每日日期、类型、时长与达标状态、当天学校道闸进出时间和方向，以及刷新状态和窗口编号。不会保存姓名、学号、门禁通道名称或完整页面内容。
- 考勤缓存使用 chrome.storage.local，不使用 chrome.storage.sync，不随源码打包。该缓存不是加密保险箱，有本机浏览器访问权限的人可能查看它。
- 升级会按允许字段重新保存旧缓存并移除扩展存储中不再使用的字段。卸载扩展可清除其本地缓存；这不会注销学校账号。学校 Cookie 可在 Chrome 的网站数据设置中自行删除。

## 网站权限

stu.slai.edu.cn：打开学校系统并读取本人考勤页面。

sts.slai.edu.cn：识别学校单点登录跳转。登录由用户在学校页面完成。

扩展自身只向上述学校域名发起查询；学校网页自己的资源和单点登录行为由学校控制。学校系统可能把会话标识放在 URL 中，浏览器正常导航可能留下历史记录，扩展不会额外申请历史记录权限或自动清除浏览器历史。

## 开源与反馈

仓库中的账号、记录和演示图均为虚构测试数据。GitHub 用于代码托管和手动下载，扩展运行时不会向 GitHub 上传数据。

提交 Issue 时请删除姓名、学号、会话 URL、Cookie、原始考勤截图和网络日志。说明现象、扩展版本与浏览器版本即可。
